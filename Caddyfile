:8080

# security {
#     authorization policy premium {
#         
#     }
# }

route /api {
    rate_limit {
        distributed
        zone static_example {
            match {
                method GET
            }
            key    static
            events 100
            window 1m
        }
        zone dynamic_example {
            key    {remote_host}
            events 2
            window 1s
        }
        log_key
    }
    respond "I'm behind the rate limiter!"
}
respond "Not limited"
